# Secrets directory

Place private service-account files here only on your machine or deployment platform. The directory is ignored by Git. Never include real API keys or Firebase credentials in a ZIP shared publicly.
