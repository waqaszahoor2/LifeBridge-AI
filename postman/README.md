# Postman

Import `LifeBridge_AI.postman_collection.json`, set `baseUrl`, and enable the bearer or `X-Admin-Key` header only for protected endpoints. Request bodies are intentionally empty placeholders; use the schemas in `docs/openapi.json` or Swagger UI.
